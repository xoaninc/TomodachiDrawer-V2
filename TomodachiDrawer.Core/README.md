# TomodachiDrawer.Core
Main fundamental logic. This is used by the UI or CLI or whatever.

At the moment, this is just the WinForms UI, but the idea behind this is that this could ideally be used by any type of UI if someone wants to contribute it!