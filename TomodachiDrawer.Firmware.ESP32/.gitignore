# ESP-IDF build artifacts
build/
managed_components/
dependencies.lock

# Generated sdkconfig (board/target-specific, regenerated from sdkconfig.defaults)
sdkconfig
sdkconfig.old
sdkconfig.ci

# Host test build output
*.exe
*.obj
*.o

# Local test .tdld fixtures (generated ad hoc, not source)
*.tdld

# IDE / editor metadata
.cache/
.vscode/
.idea/
*.swp

# Parallel-session plugin metadata (do not commit)
